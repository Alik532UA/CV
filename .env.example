# PUBLIC VARIABLES (prefixed with PUBLIC_)
# Available in client and server
PUBLIC_APP_VERSION=$npm_package_version
PUBLIC_BASE_URL=/CV

# PRIVATE VARIABLES
# Only available in server-side code (+page.server.ts, +server.ts, hooks.server.ts)
# API_SECRET_KEY=your_secret_here
