# PUBLIC VARIABLES (prefixed with PUBLIC_)
PUBLIC_APP_VERSION=$npm_package_version
PUBLIC_BASE_URL=/CV

# PRIVATE VARIABLES (Server-side API keys, never exposed to client)
GEMINI_API_KEY=your_gemini_api_key_here
