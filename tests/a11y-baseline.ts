/**
 * База для axe-аудиту (ACCESSIBILITY-v8 § 10.1.1, CODE-QUALITY-v8 § 5.5).
 *
 * ЧОМУ ЧИСЛО, А НЕ `toEqual([])`. Нуль виглядає найсуворішим порогом і
 * насправді є найкрихкішим: axe-core додає правила в МІНОРНИХ версіях, а
 * Dependabot приносить їх щотижня. Гейт із нулем червоніє від оновлення
 * залежності без жодної зміни коду — а червоний гейт, причина якого не в коді,
 * вимикають. Це той самий клас, що `off` у ESLint: поріг, який доводиться
 * вимкнути назавтра, гірший за відсутній.
 *
 * Тому база — це пара:
 *   KNOWN — перелік id правил. Поява НОВОГО типу порушення валить тест навіть у
 *           межах ліміту, тож борг не ховається за числом;
 *   COUNT — кількість. Може лише спадати, і кожне зменшення робиться разом із
 *           правкою, що його спричинила.
 *
 * Числа отримані прогоном `npm run test:e2e -- tests/a11y.spec.ts`, а не
 * оцінені (AI-AGENT-PITFALLS-v8 § 5.5).
 */

/** Типи порушень, які вже є. Новий id — падіння, навіть якщо кількість не зросла. */
export const A11Y_KNOWN: Record<string, readonly string[]> = {
	home: [],
	homeDark: [],
	aiModal: []
};

/** Кількість порушень. Рухається лише вниз. */
export const A11Y_BASELINE: Record<string, number> = {
	home: 0,
	homeDark: 0,
	aiModal: 0
};
