/**
 * База для перевірки сенсорних зон (ACCESSIBILITY-v8 § 8, § 10.3).
 *
 * ЧОМУ БАЗА, А НЕ `toEqual([])`. Правило § 8 не перевіряв ніхто, і перший же
 * замір показав 18 замалих цілей на головній і ще три в модалці. Гейт із нулем
 * тут означав би одне з двох: або переверстати мобільну шапку, панель фільтрів і
 * картки проєктів одним комітом наосліп, або вимкнути гейт — а вимкнений гейт
 * гірший за відсутній (CODE-QUALITY-v8 § 6.4.1 про `off` в ESLint,
 * ACCESSIBILITY-v8 § 10.1.1 про нуль в axe). Тому борг оформлений тим самим
 * патерном, що вже застосований у цьому проєкті для axe і для розміру файлів:
 *
 *   KNOWN — поіменний перелік. НОВА замала ціль валить прогін навіть у межах
 *           числа, тож борг не ховається за лічильником;
 *   COUNT — кількість. Рухається лише вниз, і кожне зменшення робиться разом
 *           із правкою, що його спричинила.
 *
 * Числа отримані прогоном `npx playwright test --project=chromium
 * tests/touch-targets.spec.ts` на вьюпорті 390×844, а не оцінені
 * (AI-AGENT-PITFALLS-v8 § 5.5). Розміри в назвах не зберігаються навмисно:
 * інакше база червоніла б від зміни шрифту на піксель.
 *
 * Порядок розбору, від найдешевшого до найдорожчого:
 *   1. ~~картки проєктів (245×42) і кнопка аналізу (308×42) — бракує 2px
 *      висоти~~ — ЗАКРИТО. `min-height: 44px` на `.btn-primary` і
 *      `.btn-secondary` у `src/app.css`: обидві кнопки складені з тих самих
 *      12px відступу й рядка 0.9rem, тож одне оголошення закрило дев'ять
 *      цілей — вісім карток і кнопку аналізу. `home` 18 → 10, `aiModal` 3 → 2;
 *   2. `base-modal-close-btn` (40×40) — канон називає кнопку закриття
 *      найчастішим порушником (UI-ELEMENTS-v8 § 3), і їй бракує 4px. АЛЕ:
 *      +4px тут недосить, і це виміряно, а не припущено. На 375px заголовок
 *      модалки AI — це флекс-рядок «AI Job Matcher» + `ai-model-badge-btn`, і
 *      правий край бейджа доходить до x=308 при кнопці закриття на x=304..344.
 *      Перекриття вже є (4×2px), а з 44px кнопкою стає 8×6px — тобто кут
 *      бейджа починає закривати модалку замість того, щоб відкрити вибір
 *      моделі. Спершу заголовок мусить перестати заходити під кут (симетричний
 *      `padding-inline`, від якого бейдж переїде на другий рядок), і це вже
 *      рішення про вигляд, а не про 4 пікселі;
 *   3. панель фільтрів (34px) і перемикач теми (38×32) — потрібна переверстка;
 *   4. `ai-model-badge-btn` (130×23) — найменша ціль на сайті.
 */

/** Поіменно. Нова назва — падіння, навіть якщо кількість не зросла. */
export const TOUCH_KNOWN: Record<string, readonly string[]> = {
	home: [
		'a "Skip to main content"',
		// Мобільна кнопка перемикача тла. Ім'я змінилося не тому, що змінилася
		// кнопка: її `aria-label` переїхав у словник (`t.ui.background`), і
		// англійський текст тепер приходить звідти.
		"Background effect",
		"sound-toggle-btn",
		"lang-trigger-btn",
		"theme-light-btn",
		"theme-dark-btn",
		'button "All Projects"',
		'button "Games"',
		'button "Apps"',
		'button "Websites"'
	],
	aiModal: ["base-modal-close-btn", "ai-model-badge-btn"],
	/** Нова сторінка, зроблена одразу за правилом. Порожньо — і мусить лишитися. */
	betaChecklist: []
};

/** Кількість замалих цілей. Рухається лише вниз. */
export const TOUCH_BASELINE: Record<string, number> = {
	home: 10,
	aiModal: 2,
	betaChecklist: 0
};

/**
 * Пари цілей, що наїжджають одна на одну (`A11Y-TOUCH-OVERLAP`, MEDIUM).
 *
 * Поіменно й без розмірів, з тієї самої причини, що й перелік вище: інакше
 * база червоніла б від зміни шрифту на піксель. Порядок у назві пари —
 * порядок обходу DOM, він стабільний.
 *
 * ЄДИНИЙ ЗАПИС, І ВІН НЕ «ЩЕ НЕ ДІЙШЛИ РУКИ». Перший же прогін на 390×844
 * знайшов реальне перекриття 127×39 px: плаваюча кнопка `AI Matcher`
 * (`position: fixed`, `bottom: 84px`, `right: 16px`, `z-index: 990`) лежить на
 * правій третині кнопки «PDF version» у шапці. На першому екрані телефона
 * напис у ній обрізаний візуально, а дотик у ту третину відкриває модалку AI
 * замість PDF. Перевірено знімком, а не міркуванням.
 *
 * ЧОМУ ЦЕ ЗАПИС, А НЕ ПРАВКА. Плаваюча кнопка — свідомий постійний CTA поверх
 * вмісту, що прокручується. Відступ знизу тут не рятує: він лише переносить
 * перекриття на той елемент, який опиниться під кнопкою наступним. Тобто
 * вибір не між «полагодити за п'ять хвилин» і «лишити», а між трьома
 * продуктовими рішеннями — прибирати кнопку при прокручуванні, робити її
 * значковою або зсувати саму розкладку, — і жодне з них не приймається
 * принагідним комітом.
 *
 * ЩО ЗНИЖУЄ ГОСТРОТУ. Закритий елемент не стає недосяжним назавжди: варто
 * прокрутити — і він виїжджає з-під кнопки. Тобто це втрачений дотик, а не
 * втрачена функція. Саме тому MEDIUM і саме тому запис, а не блокер.
 *
 * НОВА пара валить прогін навіть у межах цього переліку — зокрема та, заради
 * якої правило й потрапило в канон: `base-modal-close-btn` × `ai-model-badge-btn`
 * у заголовку модалки. Зараз їхній перетин 4×2 px, тобто під порогом; він
 * стане наїздом рівно тоді, коли кнопці закриття додадуть чотири пікселі
 * висоти за пунктом 2 черги вище. Тоді це побачить прогін, а не відвідувач.
 */
export const TOUCH_OVERLAP_KNOWN: Record<string, readonly string[]> = {
	home: ["hero-pdf-btn × ai-matcher-open-btn"],
	aiModal: []
};
